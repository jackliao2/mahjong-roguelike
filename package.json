{
  "name": "mahjong-quiz",
  "version": "0.1.0",
  "description": "A browser-based Mahjong Quiz for learning riichi mahjong through multiple-choice questions",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^4.1.10"
  }
}
